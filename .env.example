# ==============================================================================
# Student Portal - Root Environment Variables Reference
# ==============================================================================

# Backend Settings (see backend/.env.example)
PORT=5001
NODE_ENV=development
JWT_SECRET=portal-secure-production-jwt-secret-key-replace-in-prod
CORS_ORIGIN=*

# Frontend Settings (see frontend/.env.example)
VITE_API_BASE_URL=/api
